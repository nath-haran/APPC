<nav role="navigation" class="navbar navbar-default navbar-fixed-top navbar-inverse">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to "APPS",root_path ,class: "navbar-brand"%>
    </div>
    <!-- Collection of nav links and other content for toggling -->
    <div id="navbarCollapse" class="collapse navbar-collapse ">
        <ul class="nav navbar-nav">
            <% if(params[:controller]=='home') %>
            <li class="active">
            <% else %>
            <li>
            <% end %>
            <%= link_to "Home",root_path %></li>
            <% if(params[:controller]=='users') %>
            <li class="active">
            <% else %>
            <li>
            <% end %><%= link_to "Profile",profile_path %></li>
            <% if(params[:controller]=='companies') %>
            <li class="active">
            <% else %>
            <li>
            <% end %><%= link_to "Companies",companies_path %></li>
            </ul>
            <% if(current_user.nil?)  %>

<ul class="nav navbar-nav pull-right">
            <li class="dropdown">
            
              <a class="dropdown-toggle" data-toggle="dropdown">Login<b class="caret"></b></a>
    <ul class="dropdown-menu pull-right">
            <br>
            
            <li><%= link_to "Admin", admin_login_path %></li>
            <li><%= link_to "Student", signin_path %></li>
            
            <!--<li class="divider"></li>-->
            
        
        </ul>
        <% else %>
        <ul class="nav navbar-nav pull-right">
        <li ><%= link_to "Signout" ,signout_path , class: "pull-right"%></li>
        </ul>

            
     </li>       
        </ul>
            <% end %>
</div>        

         
    
    </div>
   
</nav>
<div>
    <br><br><br>
</div>
